using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("깃허브에 정상적으로 커밋되었습니다.");
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
